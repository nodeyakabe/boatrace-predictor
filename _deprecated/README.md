# 非推奨スクリプト（Deprecated Scripts）

このディレクトリには、ワークフロー統合前の古いスクリプトが保管されています。

## 移行履歴

### 2025-12-02: データ収集スクリプトのワークフロー統合

**統合完了機能:**
1. 不足データ取得 → `MissingDataFetchWorkflow` (src/workflow/missing_data_fetch.py)
2. 今日の予測生成 → `TodayPredictionWorkflow` (src/workflow/today_prediction.py)
3. オリジナル展示収集 → `TenjiCollectionWorkflow` (src/workflow/tenji_collection.py)

**現役の補完スクリプト:**
- `補完_レース詳細データ_改善版v4.py` - 期間フィルター対応済み
- `補完_決まり手データ_改善版.py` - 期間フィルター対応済み
- `補完_払戻金データ.py` - 期間フィルター対応済み
- `fetch_original_tenji_daily.py` - オリジナル展示収集（ワークフローから呼び出し）

## 削除ガイドライン

以下の条件を満たす場合、これらのファイルは安全に削除できます:
1. 3ヶ月以上使用されていない
2. 新しいワークフローで同等の機能が提供されている
3. バックアップが取られている

## ディレクトリ構成

```
_deprecated/
├── 補完スクリプト/     # 旧補完スクリプト（天候、風向、展示タイムなど）
└── fetchスクリプト/     # 旧データ取得スクリプト（手動実行用）
```

## 注意事項

- このディレクトリのスクリプトは**メンテナンスされていません**
- 動作保証はありません
- 緊急時の参照用としてのみ保管されています
