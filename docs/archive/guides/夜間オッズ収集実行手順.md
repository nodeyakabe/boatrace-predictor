# 夜間オッズ収集実行手順

## 📊 概要

2020～2024年の全レースオッズデータを並列処理で高速収集します。

### 処理スペック

| 項目 | 詳細 |
|------|------|
| **対象期間** | 2020-01-01 ～ 2024-12-31 |
| **対象件数** | 約78,000レース |
| **並列スレッド数** | 10 |
| **推定処理時間** | 13～20時間 |
| **推定完了時刻** | 翌朝8～15時 |

### 最適化内容

- **従来版**: 0.10件/秒 → 推定204時間（8.5日）
- **並列版**: 1.0-1.5件/秒 → 推定13-20時間
- **高速化率**: 約10-15倍

---

## 🚀 実行手順

### ステップ1: 事前確認

#### 1-1. テスト実行（推奨）

```bash
python test_quick_parallel.py
```

**期待される出力:**
```
[OK] 並列処理が正常に動作しています
```

#### 1-2. 既存プロセスの停止

タスクマネージャーで以下を確認:
- `python.exe` で「fetch_odds」が実行中の場合は停止

または:
```bash
# プロセスを確認
tasklist | findstr python

# 必要に応じて停止（タスクマネージャー推奨）
```

#### 1-3. PC設定確認

以下を確認してください:
- [ ] 電源設定: スリープしない
- [ ] ネットワーク: 安定した接続
- [ ] ディスク空き容量: 1GB以上推奨

---

### ステップ2: 夜間実行開始

#### 方法A: バッチファイルから実行（推奨）

```bash
run_odds_parallel_night.bat
```

実行後の確認質問:
1. 「夜間実行を開始しますか？ (Y/N)」→ `Y`
2. 「進捗をリアルタイム監視しますか？ (Y/N)」→ `N`（バックグラウンド実行）

#### 方法B: Pythonスクリプトから直接実行

```bash
python fetch_odds_parallel.py --workers 10 --delay 0.3 --batch-size 50
```

---

### ステップ3: 進捗確認

#### 3-1. ログファイルで確認

```bash
# 最新100行を表示
tail -n 100 logs/odds_fetch_parallel_night.log

# リアルタイム監視
tail -f logs/odds_fetch_parallel_night.log
```

#### 3-2. 進捗チェックスクリプト（既存）

```bash
python check_odds_progress.py
```

#### 3-3. 進捗ログの見方

```
進捗: 1,000/78,061 (1.3%) | 成功: 990, エラー: 10 | 速度: 1.2件/秒 | 残り: 17.8時間
```

- **進捗**: 処理済み件数/全体件数
- **成功**: 正常取得件数
- **エラー**: 失敗件数（中止レース等）
- **速度**: 現在の処理速度
- **残り**: 推定残り時間

---

## ⚙️ パラメータ調整（必要に応じて）

### さらに高速化したい場合

```bash
python fetch_odds_parallel.py --workers 15 --delay 0.2
```

⚠️ 注意: スレッド数を増やすとエラー率が上がる可能性があります

### 安定性を重視したい場合

```bash
python fetch_odds_parallel.py --workers 5 --delay 0.5
```

### バッチサイズ調整

```bash
# 大きくすると高速だがメモリ使用量増
python fetch_odds_parallel.py --batch-size 100

# 小さくすると安定だが若干遅い
python fetch_odds_parallel.py --batch-size 25
```

---

## 🔧 トラブルシューティング

### Q1: 「DB not found」エラー

**原因**: データベースパスが間違っている

**対処**:
```bash
# DBの存在確認
dir data\boatrace.db

# 存在しない場合、パスを指定
python fetch_odds_parallel.py --db "正しいパス\boatrace.db"
```

### Q2: エラー率が高い（10%以上）

**原因**: ネットワーク不安定、またはスレッド数過多

**対処**:
```bash
# スレッド数を減らして再実行
python fetch_odds_parallel.py --workers 5 --delay 0.5
```

### Q3: 処理が途中で止まった

**原因**: ネットワーク切断、PCスリープなど

**対処**:
- 再実行すれば未取得分から自動再開されます
- データ損失の心配はありません

```bash
# 同じコマンドで再実行
run_odds_parallel_night.bat
```

### Q4: 速度が遅い（0.5件/秒未満）

**原因**: ネットワーク遅延、PC負荷など

**対処**:
1. 他のアプリケーションを終了
2. ネットワーク接続を確認
3. delayパラメータを調整（0.3→0.5秒に増やす）

---

## 📝 実行後の確認

### 完了確認

ログファイルの最後に以下が表示されていれば完了:

```
================================================================================
処理完了
  成功: 77,XXX件
  エラー: XXX件
  処理時間: XX.X時間
  平均速度: X.XX件/秒
================================================================================
```

### データベース確認

```bash
# 取得件数を確認
python check_odds_progress.py
```

または直接SQL:

```sql
SELECT COUNT(*) FROM trifecta_odds;
-- 目標: 約7,800,000件（78,000レース × 平均100通り）
```

---

## 📋 チェックリスト

実行前:
- [ ] テスト実行成功（test_quick_parallel.py）
- [ ] 既存プロセス停止確認
- [ ] PC設定確認（スリープ無効化）
- [ ] ディスク容量確認

実行中:
- [ ] 開始直後の進捗ログ確認（エラー率チェック）
- [ ] 1時間後の進捗確認
- [ ] PCが稼働し続けているか確認

完了後:
- [ ] ログで完了メッセージ確認
- [ ] データベース件数確認
- [ ] エラーログ確認

---

## 🎯 期待される結果

### 成功ケース

```
処理完了
  成功: 77,500レース
  エラー: 561レース（0.7%）
  処理時間: 15.2時間
  平均速度: 1.42件/秒
  高速化率: 14.2倍（従来比）
```

### エラーについて

エラー率1%未満は正常です。以下のレースはオッズが取得できません:
- 中止レース
- 非開催日
- データ未公開レース

---

## 📞 問題が発生したら

1. ログファイルを確認: `logs/odds_fetch_parallel_night.log`
2. エラーメッセージをメモ
3. 再実行を試す（未取得分のみ処理されます）
4. 必要に応じてパラメータ調整

---

**最終更新**: 2025-12-18
**作成者**: Claude Code
