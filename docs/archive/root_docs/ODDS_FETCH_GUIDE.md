# 2020～2024年オッズデータ取得ガイド

## 📋 概要

2020年～2024年の全レースのオッズデータ（3連単）を取得するスクリプトです。

- **対象レース数**: 約78,000レース
- **推定時間**: 約10～11時間（0.5秒間隔の場合）
- **バックグラウンド実行**: 可能
- **中断・再開**: 対応（未取得のみ処理）

---

## 🚀 実行方法

### 方法1: バッチファイルで実行（推奨）

Windows環境で最も簡単な方法です。

```batch
run_odds_fetch_2020_2024.bat
```

ダブルクリックするだけでバックグラウンド実行が開始されます。

### 方法2: Pythonコマンドで実行

```bash
python fetch_odds_2020_2024_background.py
```

#### オプション指定

```bash
# 遅延時間を変更（デフォルト: 0.5秒）
python fetch_odds_2020_2024_background.py --delay 0.3

# 期間を指定
python fetch_odds_2020_2024_background.py --start-date 2020-01-01 --end-date 2020-12-31

# ログファイルを変更
python fetch_odds_2020_2024_background.py --log-file logs/my_fetch.log
```

---

## 📊 進捗確認

### 方法1: 進捗確認スクリプト

```bash
python check_odds_progress.py
```

**出力例:**
```
================================================================================
オッズ取得進捗確認
確認日時: 2025-12-17 10:30:00
================================================================================

【年別進捗】
年           全レース数         取得済み       進捗率
--------------------------------------------------
2020              9,642          5,000       51.9%
2021              9,538          3,200       33.6%
2022             35,883              0        0.0%
2023              9,084              0        0.0%
2024             13,914              0        0.0%
--------------------------------------------------
合計             78,061          8,200       10.5%

残りレース数: 69,861件
推定残り時間（0.5秒/レース）: 582.2分 (9.7時間)

【最近取得したレース（最新5件）】
  2021-06-15 01R5 - 取得: 2025-12-17 10:29:58
  2021-06-15 01R4 - 取得: 2025-12-17 10:29:52
  ...
```

### 方法2: ログファイルを確認

```bash
# Windowsコマンドプロンプト
type logs\odds_fetch_2020_2024.log

# 最新の20行のみ表示
powershell Get-Content logs\odds_fetch_2020_2024.log -Tail 20
```

**ログ例:**
```
2025-12-17 10:00:00 [INFO] 2020～2024年オッズデータ取得開始
2025-12-17 10:00:05 [INFO] 対象レース数: 78,061件
2025-12-17 10:10:00 [INFO] 進捗: 1,000/78,061 (1.3%) | 成功: 995, エラー: 5 | 速度: 1.67件/秒 | 残り: 12.8時間
```

---

## ⏸️ 停止・再開

### 停止方法

1. **タスクマネージャー**を開く（Ctrl + Shift + Esc）
2. 「Python」プロセスを探す
3. プロセスを選択して「タスクの終了」

### 再開方法

同じコマンドを再実行するだけで、**未取得のレースのみ**自動的に処理されます。

```bash
python fetch_odds_2020_2024_background.py
```

---

## 📁 ファイル構成

```
BoatRace/
├── fetch_odds_2020_2024_background.py  # メインスクリプト
├── run_odds_fetch_2020_2024.bat        # Windows用起動バッチ
├── check_odds_progress.py              # 進捗確認スクリプト
├── ODDS_FETCH_GUIDE.md                 # このファイル
└── logs/
    └── odds_fetch_2020_2024.log        # ログファイル
```

---

## ⚙️ パラメータ一覧

| パラメータ | デフォルト | 説明 |
|-----------|----------|------|
| `--db` | `data/boatrace.db` | データベースパス |
| `--start-date` | `2020-01-01` | 開始日 |
| `--end-date` | `2024-12-31` | 終了日 |
| `--delay` | `0.5` | リクエスト間隔（秒） |
| `--log-file` | `logs/odds_fetch_2020_2024.log` | ログファイルパス |
| `--progress-interval` | `100` | 進捗表示間隔（レース数） |

---

## 🔧 トラブルシューティング

### エラー: DB not found

**原因**: データベースファイルが見つからない

**解決策**:
```bash
# DBパスを確認
python -c "import os; print(os.path.exists('data/boatrace.db'))"
```

### 取得速度が遅い

**原因**: サーバー側の制限またはネットワーク遅延

**解決策**:
- `--delay`を増やす（0.5秒 → 1.0秒）
- 時間帯を変える（深夜など）

### 途中でエラーが多発

**原因**: ネットワーク不安定またはサーバー側の問題

**解決策**:
1. スクリプトを一旦停止
2. しばらく待つ（30分～1時間）
3. 再開する（未取得分のみ処理される）

---

## 📝 推奨設定

### 夜間実行（おすすめ）

```bash
# 0.5秒間隔で実行（推定10～11時間）
python fetch_odds_2020_2024_background.py --delay 0.5
```

- 就寝前に実行
- 朝起きたら完了している
- サーバー負荷も分散

### 高速実行（注意）

```bash
# 0.3秒間隔で実行（推定6～7時間）
python fetch_odds_2020_2024_background.py --delay 0.3
```

⚠️ サーバーに負荷がかかるため、エラーが多発する可能性があります

### 安全実行

```bash
# 1.0秒間隔で実行（推定21～22時間）
python fetch_odds_2020_2024_background.py --delay 1.0
```

✅ 最も安全だが時間がかかる

---

## 📞 サポート

問題が発生した場合:

1. ログファイルを確認 (`logs/odds_fetch_2020_2024.log`)
2. 進捗確認スクリプトを実行 (`python check_odds_progress.py`)
3. エラーメッセージを記録

---

**作成日**: 2025-12-17
**最終更新**: 2025-12-17
