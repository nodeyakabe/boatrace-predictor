# 夜間バッチ - 不足データ一括取得 実行手順

## 🎯 スクリプト選択ガイド

### bulk_missing_data_fetch.py（標準版）
- **対象期間:** 過去30日（デフォルト）
- **推定時間:** 15-30分
- **用途:** 定期メンテナンス、最近のデータ補完

### bulk_missing_data_fetch_12h.py（12時間バッチ版）⭐
- **対象期間:** 全期間（戦略的優先順位付き）
- **推定時間:** 最大12時間
- **用途:** 大規模データ補完、初回セットアップ
- **特徴:**
  - 3フェーズ実行（優先度順）
  - 中断・再開機能付き
  - 進捗自動保存

## 📋 事前準備

### 1. 現在の不足データ状況を確認

#### 過去30日の状況（標準版向け）

```bash
python -c "
import sqlite3
import sys
sys.stdout.reconfigure(encoding='utf-8')
from datetime import datetime, timedelta

db_path = 'data/boatrace.db'
conn = sqlite3.connect(db_path)
cursor = conn.cursor()

end_date = datetime.now().date()
start_date = end_date - timedelta(days=30)

print(f'=== 不足データ分析 (過去30日) ===')
print(f'期間: {start_date} ～ {end_date}\n')

# 結果データ欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.race_date)
    FROM races r
    WHERE r.race_date BETWEEN ? AND ?
      AND r.race_date < date('now')
      AND r.id NOT IN (SELECT DISTINCT race_id FROM results)
''', (str(start_date), str(end_date)))
missing_results = cursor.fetchone()[0]

# 展示タイム欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.race_date)
    FROM races r
    LEFT JOIN race_details rd ON r.id = rd.race_id
    WHERE r.race_date BETWEEN ? AND ?
      AND r.race_date < date('now')
      AND (rd.exhibition_time IS NULL OR rd.exhibition_time = 0)
''', (str(start_date), str(end_date)))
missing_exhibition = cursor.fetchone()[0]

# 決まり手欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.race_date)
    FROM races r
    JOIN results res ON r.id = res.race_id
    WHERE r.race_date BETWEEN ? AND ?
      AND r.race_date < date('now')
      AND res.rank = '1'
      AND res.kimarite IS NULL
''', (str(start_date), str(end_date)))
missing_kimarite = cursor.fetchone()[0]

# 払戻金欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.race_date)
    FROM races r
    JOIN results res ON r.id = res.race_id
    WHERE r.race_date BETWEEN ? AND ?
      AND r.race_date < date('now')
      AND res.is_invalid = 0
      AND r.id NOT IN (SELECT DISTINCT race_id FROM payouts)
''', (str(start_date), str(end_date)))
missing_payouts = cursor.fetchone()[0]

print('【不足データ種別】')
print(f'  結果データ欠損: {missing_results}日')
print(f'  展示タイム欠損: {missing_exhibition}日')
print(f'  決まり手欠損: {missing_kimarite}日')
print(f'  払戻金欠損: {missing_payouts}日')

conn.close()
"
```

#### 全期間の状況（12時間バッチ版向け）

```bash
python -c "
import sqlite3
import sys
sys.stdout.reconfigure(encoding='utf-8')

db_path = 'data/boatrace.db'
conn = sqlite3.connect(db_path)
cursor = conn.cursor()

print('=== 不足データ分析 (全期間) ===\n')

# 結果データ欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.id), COUNT(DISTINCT r.race_date)
    FROM races r
    WHERE r.race_date < date('now')
      AND r.id NOT IN (SELECT DISTINCT race_id FROM results)
''')
missing_results = cursor.fetchone()

# 展示タイム欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.id), COUNT(DISTINCT r.race_date)
    FROM races r
    LEFT JOIN race_details rd ON r.id = rd.race_id
    WHERE r.race_date < date('now')
      AND (rd.exhibition_time IS NULL OR rd.exhibition_time = 0)
''')
missing_exhibition = cursor.fetchone()

# 決まり手欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.id), COUNT(DISTINCT r.race_date)
    FROM races r
    JOIN results res ON r.id = res.race_id
    WHERE r.race_date < date('now')
      AND res.rank = '1'
      AND res.kimarite IS NULL
''')
missing_kimarite = cursor.fetchone()

# 払戻金欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.id), COUNT(DISTINCT r.race_date)
    FROM races r
    JOIN results res ON r.id = res.race_id
    WHERE r.race_date < date('now')
      AND res.is_invalid = 0
      AND r.id NOT IN (SELECT DISTINCT race_id FROM payouts)
''')
missing_payouts = cursor.fetchone()

print('【不足データ種別】')
print(f'  結果データ欠損: {missing_results[0]:,}レース ({missing_results[1]}日)')
print(f'  展示タイム欠損: {missing_exhibition[0]:,}レース ({missing_exhibition[1]}日)')
print(f'  決まり手欠損: {missing_kimarite[0]:,}レース ({missing_kimarite[1]}日)')
print(f'  払戻金欠損: {missing_payouts[0]:,}レース ({missing_payouts[1]}日)')
print(f'\n合計: {missing_results[0] + missing_exhibition[0] + missing_kimarite[0] + missing_payouts[0]:,}レース')

conn.close()
"
```

## 🚀 実行方法

### 標準版（bulk_missing_data_fetch.py）

#### オプション1: デフォルト実行（過去30日）

```bash
python scripts/bulk_missing_data_fetch.py
```

#### オプション2: 日数指定

```bash
# 過去60日分
python scripts/bulk_missing_data_fetch.py --days 60

# 過去7日分
python scripts/bulk_missing_data_fetch.py --days 7
```

#### オプション3: 期間指定

```bash
# 特定期間を指定
python scripts/bulk_missing_data_fetch.py --start-date 2025-11-01 --end-date 2025-12-01
```

### 12時間バッチ版（bulk_missing_data_fetch_12h.py）

#### オプション1: フル実行（全期間・全データ種）⚠️

```bash
python scripts/bulk_missing_data_fetch_12h.py
```

**注意:** 全期間の全データ種を対象とするため、12時間では完了しない可能性があります。

#### オプション2: 2024年以降のみ（推奨）

```bash
python scripts/bulk_missing_data_fetch_12h.py --recent-only
```

**推奨理由:**
- フェーズ1-2: 決まり手・払戻金・結果データは全期間対象（1-2時間）
- フェーズ3: 直前情報は2024年以降のみ（2-4時間）
- 合計3-6時間で完了見込み

#### オプション3: 期間指定

```bash
# 直前情報の対象期間を指定
python scripts/bulk_missing_data_fetch_12h.py --start-date 2024-01-01 --end-date 2025-12-02
```

#### オプション4: 中断後の再開

```bash
# 前回の続きから再開
python scripts/bulk_missing_data_fetch_12h.py --resume
```

**進捗保存:** `temp/batch_progress_12h.json` に自動保存されます。

## 📊 処理フロー

### 標準版の処理フロー（過去30日）

#### フェーズ1: 当日確定情報（結果データに依存）
1. 結果データ取得
2. レース詳細（ST/コース）補完
3. 決まり手補完
4. 払戻金補完

#### フェーズ2: 直前情報（結果に依存しない）
1. 展示タイム取得
2. チルト取得
3. 調整重量取得
4. 天候・風向取得
5. オリジナル展示取得
6. 潮位データ取得

### 12時間バッチ版の処理フロー（戦略的優先順位）

#### フェーズ1: 🔴 最高優先度（全期間）
- **対象:** 決まり手・払戻金
- **理由:** 予測精度に直結する最重要データ
- **推定時間:** 30-60分
- **対象レース数:** 約10,000レース

#### フェーズ2: 🟠 高優先度（全期間）
- **対象:** 結果データ・レース詳細（ST/コース）
- **理由:** 全データの基礎となる情報
- **推定時間:** 1-2時間
- **対象レース数:** 約2,500レース（結果）+ α（詳細）

#### フェーズ3: 🟡 中優先度（全期間または2024年以降）
- **対象:** 展示タイム、チルト、調整重量、天候、潮位
- **理由:** 重要だが古いデータの優先度は低い
- **推定時間:**
  - 2024年以降のみ: 2-4時間
  - 全期間: 8-10時間
- **対象レース数:**
  - 2024年以降: 約30,000-40,000レース
  - 全期間: 約160,000レース

## ⏱️ 推定実行時間

### 標準版
- **過去30日（11-12日分の欠損）:** 約15-30分
- **過去60日:** 約30-60分

### 12時間バッチ版
- **--recent-only（推奨）:** 3-6時間
- **全期間（フル）:** 10-13時間（12時間では未完の可能性あり）

※ネットワーク状況により変動

## 📝 実行ログ

ログは標準出力に表示されます。ファイルに保存する場合：

```bash
python scripts/bulk_missing_data_fetch.py > logs/batch_$(date +%Y%m%d_%H%M%S).log 2>&1
```

Windowsの場合：

```bash
python scripts/bulk_missing_data_fetch.py > logs/batch_%date:~0,4%%date:~5,2%%date:~8,2%_%time:~0,2%%time:~3,2%%time:~6,2%.log 2>&1
```

## ✅ 実行後の確認

### 1. 実行結果を確認

スクリプトが最後に表示する「最終サマリー」を確認：
- ステータス（✅ 成功 / ❌ 失敗）
- 処理数
- エラー数

### 2. データベースを再確認

```bash
python -c "
import sqlite3
import sys
sys.stdout.reconfigure(encoding='utf-8')
from datetime import datetime, timedelta

db_path = 'data/boatrace.db'
conn = sqlite3.connect(db_path)
cursor = conn.cursor()

end_date = datetime.now().date()
start_date = end_date - timedelta(days=30)

print('=== 実行後の不足データ確認 ===\n')

# 結果データ欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.race_date)
    FROM races r
    WHERE r.race_date BETWEEN ? AND ?
      AND r.race_date < date('now')
      AND r.id NOT IN (SELECT DISTINCT race_id FROM results)
''', (str(start_date), str(end_date)))
print(f'結果データ欠損: {cursor.fetchone()[0]}日')

# 展示タイム欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.race_date)
    FROM races r
    LEFT JOIN race_details rd ON r.id = rd.race_id
    WHERE r.race_date BETWEEN ? AND ?
      AND r.race_date < date('now')
      AND (rd.exhibition_time IS NULL OR rd.exhibition_time = 0)
''', (str(start_date), str(end_date)))
print(f'展示タイム欠損: {cursor.fetchone()[0]}日')

# 決まり手欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.race_date)
    FROM races r
    JOIN results res ON r.id = res.race_id
    WHERE r.race_date BETWEEN ? AND ?
      AND r.race_date < date('now')
      AND res.rank = '1'
      AND res.kimarite IS NULL
''', (str(start_date), str(end_date)))
print(f'決まり手欠損: {cursor.fetchone()[0]}日')

# 払戻金欠損
cursor.execute('''
    SELECT COUNT(DISTINCT r.race_date)
    FROM races r
    JOIN results res ON r.id = res.race_id
    WHERE r.race_date BETWEEN ? AND ?
      AND r.race_date < date('now')
      AND res.is_invalid = 0
      AND r.id NOT IN (SELECT DISTINCT race_id FROM payouts)
''', (str(start_date), str(end_date)))
print(f'払戻金欠損: {cursor.fetchone()[0]}日')

conn.close()
"
```

## 🔧 トラブルシューティング

### エラー: ModuleNotFoundError

```bash
# プロジェクトルートで実行されているか確認
cd c:\Users\User\Desktop\BR\BoatRace_package_20251115_172032
python scripts/bulk_missing_data_fetch.py
```

### エラー: timeout

ネットワークタイムアウトの場合は再実行してください。既に取得済みのデータはスキップされます。

### 途中でキャンセルしたい場合

`Ctrl+C` で安全に中断できます。既に取得したデータは保存されています。

## 📅 定期実行（オプション）

### Windowsタスクスケジューラ

1. タスクスケジューラを開く
2. 「基本タスクの作成」を選択
3. トリガー: 毎日 23:00
4. 操作: プログラムの開始
   - プログラム: `python`
   - 引数: `scripts/bulk_missing_data_fetch.py --days 7`
   - 開始: `c:\Users\User\Desktop\BR\BoatRace_package_20251115_172032`

## 💡 ベストプラクティス

1. **実行タイミング:** 夜間（23:00以降）推奨
2. **頻度:** 週1回程度で十分
3. **対象期間:** 過去30日で十分（それ以上古いデータは通常不要）
4. **ログ保存:** 重要な実行時は必ずログを保存

## 📞 サポート

問題が発生した場合:
1. ログを確認
2. データベースファイルのバックアップを確認
3. 再実行を試す
